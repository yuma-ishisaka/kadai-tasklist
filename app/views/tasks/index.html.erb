<h1>タスクリスト</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %> &gt; <%= task.status %></li>
  <% end %>
</ul>
<%= paginate @messages %>

<%= link_to 'タスクの追加', new_task_path %>